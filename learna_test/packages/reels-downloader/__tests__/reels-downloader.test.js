'use strict';

const reelsDownloader = require('..');
const assert = require('assert').strict;

assert.strictEqual(reelsDownloader(), 'Hello from reelsDownloader');
console.info('reelsDownloader tests passed');
